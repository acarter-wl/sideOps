mayastor:
  csi:
    node:
      initContainers:
        enabled: false

loki-stack:
  # -- Enable loki log collection for our components
  enabled: false
engines:
  local:
    lvm:
      enabled: false
    zfs:
      enabled: false
