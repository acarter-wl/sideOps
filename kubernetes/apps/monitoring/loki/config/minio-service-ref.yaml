apiVersion: v1
kind: Service
metadata:
  name: minio-service-ref
  namespace: monitoring
spec:
  type: ExternalName
  externalName: minio.storage.svc.cluster.local
  ports:
  - port: 9000
    targetPort: 9000
    protocol: TCP
    name: api