apiVersion: v1
kind: Service
metadata:
  name: loki-minio
  namespace: monitoring
spec:
  type: ExternalName
  externalName: minio.storage.svc.cluster.local
  ports:
  - port: 9000
    targetPort: 9000
    protocol: TCP
    name: api