apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: tailscale-oauth
  namespace: tailscale
spec:
  provider: kubernetes
  parameters:
    objects: |
      - objectName: operator-oauth
        objectType: secret
