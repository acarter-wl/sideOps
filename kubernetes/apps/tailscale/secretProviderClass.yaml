apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: tailscale-oauth # Must match the name in values.yaml
  namespace: tailscale # Must be in the same namespace as the Pod (Tailscale Operator)
spec:
  provider: kubernetes # Using the built-in Kubernetes provider for the CSI driver
  parameters:
    objects: |
      - objectName: operator-oauth # The K8s Secret created by your ExternalSecret
        objectType: "secret"
    # The CSI driver will mou
