apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: velero-secret-generator
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - ./cloud-credentials.sops.yaml
