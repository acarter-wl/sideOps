apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vector
  namespace: argo-system
  annotations:
    argocd.argoproj.io/sync-wave: '0'
  labels:
    argocd.argoproj.io/instance: vector
spec:
  project: infrastructure
  sources:
    - repoURL: "https://github.com/acarter-wl/sideops.git"
      path: kubernetes/apps/monitoring/vector
      targetRevision: main
      ref: repo
    - repoURL: https://helm.vector.dev
      chart: vector
      targetRevision: 0.42.1
      helm:
        releaseName: vector
        valueFiles:
          - $repo/kubernetes/apps/monitoring/vector/values.yaml
  destination:
    name: in-cluster
    namespace: monitoring
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
