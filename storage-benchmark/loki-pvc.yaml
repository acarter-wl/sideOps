---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-pvc-loki
  namespace: monitoring
  labels:
    app: loki
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 64Gi
