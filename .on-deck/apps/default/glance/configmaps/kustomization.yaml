apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - env-configmap.yaml
  - glance-cm0-configmap.yaml
  - glance-cm1-configmap1.yaml
commonLabels:
  app.kubernetes.io/name: glance
  app.kubernetes.io/part-of: glance
