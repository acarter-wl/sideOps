apiVersion: v1
kind: Pod
metadata:
  name: disk-wipe-job-n5
spec:
  restartPolicy: Never
  nodeName: n5
  containers:
    - name: disk-wipe
      image: busybox
      securityContext:
        privileged: true
      command: ["/bin/sh", "-c", "dd if=/dev/zero bs=1M count=15360 oflag=direct of=/dev/sdb status=progress"]
